/** Node for an AVL tree; height is maintained as an invariant. */
public class AVLNode { int key, height; AVLNode left,right; AVLNode(int key){this.key=key;} public static void main(String[] args){System.out.println("AVLNode stores key, children, and height.");}}
